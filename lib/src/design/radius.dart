/// Border radius design tokens for CrackDetectX
/// Based on COMPLETE_PROMPT.md specifications
class AppRadius {
  static const double r6 = 6.0;    // Small elements
  static const double r10 = 10.0;  // Small elements / inputs fallback
  static const double r12 = 12.0;  // Buttons / Inputs (primary)
  static const double r14 = 14.0;  // Standard elements
  static const double r16 = 16.0;  // Standard cards
  static const double r20 = 20.0;  // Large cards / Premium elements
  static const double r24 = 24.0;  // Premium cards / Modals
  static const double r28 = 28.0;  // Extra large
  static const double r40 = 40.0;  // Extra extra large / Pills
  static const double pill = 9999.0; // Full pill shape
}
